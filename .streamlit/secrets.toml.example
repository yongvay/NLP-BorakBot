# Copy to .streamlit/secrets.toml and change the password.
#
# secrets.toml is gitignored (.gitignore:77); this example is committed so a
# fresh clone knows what to create. Never put the real password in this file.

admin_password = "change-me"